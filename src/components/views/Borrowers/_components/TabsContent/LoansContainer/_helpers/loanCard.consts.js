/* eslint-disable import/prefer-default-export */
import { Signal } from '@fyclabs/tools-fyc-react/signals';

export const $loanWatchScoreBreakdowns = Signal({
  breakdowns: {},
  isLoading: false,
});
